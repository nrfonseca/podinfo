apiVersion: batch/v1
kind: Job
metadata:
  generateName: entry-tests-
  annotations:
    argocd.argoproj.io/hook: PostSync
spec:
  template:
    spec:
      containers:
      - name: nats
        image: nats:latest
        command: [sh, -c]
        args: 
          - |
            echo "Running entry tests"
      restartPolicy: Never
  backoffLimit: 4